const student = ['John', 'Alex', 'Tom', 'Mariah'];
const grades = [4, 9, 0.5, 7];

const infoList = [student, grades];

console.log(
    `The student number 1 on the list is: ${infoList[0][1]}, and their grade is: ${infoList[1][1]}.`
);

console.log(infoList[0]);
